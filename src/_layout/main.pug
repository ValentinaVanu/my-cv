doctype html
html
  head
    meta(charset="utf-8")
    //-data.resume.page.pageTitle is equal to
    //- src, _data, resume.json, page, pageTitle
    title #{data.resume.page.pageTitle}
    //- The main website will be hosted at http://my-resume.bitbee.uk
    //- and it was a deploed copy at github.io. To avoid content duplication
    //- detected y seach engines we introduce link canonical
    //- to set the main source of data and point search engine to it
    link(rel="canonical", href="htpp://my-resume.bitbee.uk")
      
    //- grab the pageDescription from src, _data, resume.json,
    //- page and insert it into meta description for SEO
    meta(name="description",content=data.resume.page.pageDescription)
    meta(name="viewport", content="width=device-width, initial-scale=1.0, mazimum-scale=1.0, user-scaletable=no")
    //- style.css will be compiled into style.css
    //- so we make use of it below
    //- the value of config.entry.css.external can be found in package.json
    //- and you can navigate to the "config" property, down to "entry", and
    //- into "css" and finally to "external" and find the value to be "true"
    //- or "false"
    if config.entry.css.external
      link(rel="stylesheet", href="style.css")
  body
    .container
      block content
